# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`internal-link-analyzer` is an early-stage project. As of now the repository contains only a README; no build system, language toolchain, or source files have been added yet.

When the project is bootstrapped, update this file with:
- Build, lint, and test commands (including how to run a single test)
- The chosen language/framework and any non-obvious architectural decisions
